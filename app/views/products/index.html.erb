<h1>All Products</h1>

<ul>
  <% @products.each do |product| %>
    <li>
      <%= product.title %> - $<%= product.price %> 
      (Category: <%= product.category.name %>)
    </li>
  <% end %>
</ul>


